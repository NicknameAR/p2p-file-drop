version: "3.9"

services:
  fileserver:
    build: .
    container_name: fileserver
    ports:
      - "9999:9999"
    environment:
      PORT: "9999"
      JWT_SECRET: "mysecret"
      UPLOAD_DIR: "/app/uploads"
    volumes:
      - ./uploads:/app/uploads
